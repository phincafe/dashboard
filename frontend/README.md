# Square Reports React Frontend (Render-ready)

Vite + React SPA that talks to the Express backend.

## Local dev

```bash
cd frontend
npm install
npm run dev
```

Create a `.env` file in `frontend` with:

```bash
VITE_API_BASE_URL=http://localhost:4000
```

Then open http://localhost:5173
